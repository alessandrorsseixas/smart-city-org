apiVersion: v1
kind: Secret
metadata:
  name: mcp-server-secrets
  namespace: smartcity
  labels:
    app: mcp-server
    component: mcp
    environment: development
stringData:
  # Chave de API para provedor LLM/external service (ex: OpenAI) - não deixe em repositório em produção
  MCP_API_KEY: "changeme"
